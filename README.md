# Telegram Mini App на базі World Bank API

🌍 **Світовий Аналітик** — інтерактивна Telegram міні-аплікація для аналізу економічних та соціальних показників країн світу.

## 🎯 Основні можливості

✅ **Вибір країни** — Оберіть до 3 країн для порівняння з повного списку 180+ країн

✅ **Показники по категоріях**:
- 📊 **Економіка**: ВВП, ВВП на душу населення, інфляція, державний борг
- 👥 **Демографія**: населення, народжуваність, смертність
- 🏫 **Соціальні**: грамотність, освіта, безробіття, доступ до електрики
- 🌱 **Екологія**: викиди CO₂, енергоспоживання, лісова площа

✅ **Візуалізація даних** — Інтерактивні графіки з Chart.js, що показують тренди за 20+ років

✅ **Порівняння** — Аналізуйте 2-3 країни одночасно, порівнюйте статистику

✅ **Улюблені** — Зберігайте найчастіше використовувані комбінації запитів

✅ **Інтелектуальне кешування** — Швидкий доступ до даних без затримок

## 🏗️ Архітектура проекту

```
bank/
├── worker/
│   └── index.js              # Cloudflare Worker API
├── src/
│   ├── components/
│   │   ├── CountrySelector.jsx
│   │   ├── IndicatorSelector.jsx
│   │   ├── DataVisualization.jsx
│   │   ├── Comparison.jsx
│   │   └── Favorites.jsx
│   ├── utils/
│   │   └── api.js            # API client з кешуванням
│   ├── App.jsx               # Головний компонент
│   ├── main.jsx              # Entry point
│   └── index.css             # Стилі (мобільний-friendly)
├── public/
│   └── index.html            # HTML шаблон
├── package.json              # NPM залежності
├── wrangler.toml             # Конфігурація Cloudflare Workers
├── vite.config.js            # Конфігурація Vite
└── README.md                 # Цей файл
```

## 🚀 Швидкий старт

### Передумови
- Node.js 16+
- npm або yarn
- Cloudflare Account (безкоштовний)

### Установка та розробка

```bash
# 1. Клонуйте репозиторій
git clone <repo-url>
cd bank

# 2. Встановіть залежності
npm install

# 3. Налаштуйте Cloudflare
# Добавте ваш Account ID в wrangler.toml
wrangler login

# 4. Запустіть локальний сервер (dev mode)
npm run dev

# Додатково: запустіть фронтенд та worker одночасно
npm run dev:full
```

Аплікація буде доступна на `http://localhost:5173` (фронтенд) та `http://localhost:8787` (API worker).

## 📦 Розгортання на Cloudflare

### 1. Підготовка

```bash
# 1. Встановіть Wrangler
npm install -g @cloudflare/wrangler

# 2. Увійдіть в Cloudflare
wrangler login

# 3. Отримайте ваш Account ID
# Виконайте команду та скопіюйте account_id
wrangler whoami
```

### 2. Налаштування wrangler.toml

Відредагуйте `wrangler.toml` та додайте ваш Account ID:

```toml
account_id = "ваш_account_id_тут"
```

### 3. Розгортання

```bash
# Зберіть фронтенд
npm run build

# Розгорніть на Cloudflare
npm run deploy
```

Worker буде розгорнений на:
```
https://world-bank-miniapp.<your-subdomain>.workers.dev
```

## 🔧 Технічний стек

| Частина | Технологія |
|---------|------------|
| **Frontend** | React 18, Vite, Chart.js |
| **Backend** | Cloudflare Workers, Itty Router |
| **API** | World Bank Open Data REST API |
| **Стилі** | CSS3 з CSS Variables |
| **Кешування** | In-memory cache з TTL (1 час) |
| **Мобільність** | Telegram WebApp SDK |

## 📊 Використовуємі показники World Bank API

### Економічні показники
- `NY.GDP.MKTP.CD` — ВВП (поточна США $)
- `NY.GDP.PCAP.CD` — ВВП на душу населення
- `FP.CPI.TOTL.ZG` — Інфляція (% річних)
- `GC.DOD.TOTL.GD.ZS` — Державний борг (% ВВП)

### Демографічні показники
- `SP.POP.TOTL` — Населення всього
- `SP.DYN.CBRT.IN` — Народжуваність (на 1000)
- `SP.DYN.CDRT.IN` — Смертність (на 1000)

### Соціальні показники
- `SE.ADT.LITR.ZS` — Грамотність дорослих
- `SL.UEM.TOTL.ZS` — Безробіття
- `EG.ELC.ACCS.ZS` — Доступ до електрики

### Екологічні показники
- `EN.ATM.CO2E.KT` — Викиди CO₂
- `EN.ATM.CO2E.PC` — Викиди CO₂ на душу
- `EG.USE.COMM.FO.ZS` — Енергія від палива

## 🔌 API Endpoints

Усі запити до `/api/*` проксіюються на Cloudflare Worker:

```
GET  /api/countries              # Список всіх країн
GET  /api/indicators             # Список показників
GET  /api/data/:country/:indicator   # Дані країни по показнику
POST /api/compare                # Порівняння 2-3 країн
GET  /api/health                 # Перевірка статусу
```

### Приклад запиту для порівняння:

```javascript
const response = await fetch('/api/compare', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    countries: ['USA', 'UKR', 'DEU'],
    indicator: 'NY.GDP.MKTP.CD'
  })
})
const data = await response.json()
```

## 💾 Кешування даних

Аплікація використовує простий in-memory cache з 1-годинним TTL:

```javascript
// Автоматичне кешування на клієнті
const countries = await apiClient.getCountries()  // Кешується на 1 час

// Очистити кеш вручну
apiClient.clearCache()
```

## 📱 Інтеграція з Telegram

### Додавання в Telegram

1. Напишіть BotFather (`@BotFather` в Telegram)
2. Виконайте `/newapp`
3. Вкажіть URL вашого деплоєного Worker:
   ```
   https://world-bank-miniapp.<your-subdomain>.workers.dev
   ```
4. Отримайте посилання на Mini App та додайте його в меню бота

### Telegram WebApp SDK

Аплікація автоматично інітиалізує Telegram WebApp SDK:

```javascript
// Розширення на весь екран
window.Telegram.WebApp.expand()

// Отримання даних користувача
const user = window.Telegram.WebApp.initDataUnsafe.user
```

## 🎨 UI/UX особливості

- 🎯 **Мінімалістичний дизайн** — Фокус на даних
- 📱 **Мобільна адаптивність** — Ідеально на смартфонах
- 🌈 **Темна тема** — Легко на очах
- ⚡ **Швидкі взаємодії** — Миттєві відповіді
- 🎭 **Інтуїтивна навігація** — 3 основні розділи

## 🔐 Безпека та приватність

- ✅ Всі запити йдуть напрямо до World Bank API
- ✅ Дані кешуються локально на клієнті (браузер)
- ✅ Без збору особистої інформації користувачів
- ✅ CORS включено для всіх origen (public API)

## 📈 Оптимізація

### Cloudflare Workers
- **Глобальна мережа** — Baixка затримка з будь-яким miejscем
- **Безкоштовний тариф** — до 100,000 запитів/день
- **Автоматичне масштабування** — Без збоїв під навантаженням

### Frontend
- **Vite bundle** — Мінімальний розмір JS (~150KB)
- **React оптимізація** — Тільки необхідні компоненти
- **CSS тюнінг** — Критичні стилі inline

## 🐛 Розв'язування проблем

### Помилка: "Account ID не знайдений"
```bash
# Отримайте ваш Account ID
wrangler whoami

# Оновіть wrangler.toml
account_id = "your_account_id_here"
```

### Помилка: CORS при запиті до API
```javascript
// Все працює автоматично через Cloudflare Worker
// Всі CORS заголовки налаштовані в worker/index.js
```

### World Bank API повернув 404
```javascript
// Деякі показники недоступні для деяких країн
// Аплікація автоматично фільтрує пусті результати
```

## 🌍 Приклади використання

### Сценарій 1: Порівняння розвитків ВВП
1. Оберіть Україну, Польщу, Німеччину
2. Виберіть показник "ВВП на душу населення"
3. Побачите графік за 20+ років
4. Зберіжте в улюбленому для швидкого доступу

### Сценарій 2: Аналіз екологічної ситуації
1. Оберіть свою країну
2. Перейдіть до Екологічних показників
3. Порівняйте викиди CO₂ за роки
4. Робіть висновки про тренди

## 📚 Документація

- [World Bank API Docs](https://datahelpdesk.worldbank.org/knowledgebase/articles/889386)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Cloudflare Workers](https://developers.cloudflare.com/workers/)
- [Chart.js Documentation](https://www.chartjs.org/)

## 📄 Ліцензія

MIT License — Вільно використовуйте в особистих та комерційних проектах.

## 🤝 Внески

Пулл-реквести вітаються! Для великих змін відкрийте issue спочатку.

## 💬 Контакти та підтримка

Виникли питання або проблеми? Напишіть issue на GitHub!

---

**Розроблено з ❤️ для цікавих даних про світ**

🚀 Happy analyzing! 📊
